name: Build OpenSTA for Windows

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*' # Triggers on version tags like v1.0.0
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup vcpkg & Cache
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: ${{ github.workspace }}/vcpkg
          vcpkgGitCommitId: 'latest'

      - name: Install Dependencies via vcpkg
        run: |
          vcpkg install tcl:x64-windows cudd:x64-windows bison:x64-windows flex:x64-windows

      - name: Configure CMake
        run: |
          cmake -B build `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_TOOLCHAIN_FILE="${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET=x64-windows `
            -DSTA_GUI=OFF

      - name: Build OpenSTA
        run: |
          cmake --build build --config Release --parallel

      - name: Verify Build
        run: |
          if (Test-Path "build\app\Release\sta.exe") {
            Write-Output "SUCCESS: OpenSTA Compiled Successfully!"
            .\build\app\Release\sta.exe -version
          } else {
            Write-Error "ERROR: sta.exe binary not found inside build\app\Release\"
            exit 1
          }

      - name: Package Release Assets
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          New-Item -ItemType Directory -Force -Path staging
          Copy-Item "build\app\Release\sta.exe" -Destination "staging\"
          if (Test-Path "build\app\Release\*.dll") {
            Copy-Item "build\app\Release\*.dll" -Destination "staging\"
          }
          Compress-Archive -Path "staging\*" -DestinationPath "OpenSTA-Windows-x64.zip" -Force

      - name: Upload Build Artifacts
        if: "!startsWith(github.ref, 'refs/tags/')"
        uses: actions/upload-artifact@v4
        with:
          name: OpenSTA-Windows-x64
          path: |
            build/app/Release/sta.exe
            build/app/Release/*.dll

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: OpenSTA-Windows-x64.zip
          name: Release ${{ github.ref_name }}
          body: |
            Automated Windows x64 build for OpenSTA version ${{ github.ref_name }}.
            
            ### Assets Included
            * `OpenSTA-Windows-x64.zip`: Contains the standalone executable (`sta.exe`) and necessary system dependencies.
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
